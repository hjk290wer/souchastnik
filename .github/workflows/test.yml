name: Test build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  dictionaries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Validate dictionaries
        run: python tools/validate_dictionaries.py
      - name: Validate internet scope
        run: python tools/relevance_gate.py

  android:
    needs: dictionaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false
      - name: Bootstrap pinned llama.cpp
        shell: bash
        run: |
          set -euo pipefail
          pin="$(sed -n 's/^PIN=//p' tools/llama-cpp-pin.txt | tail -n1)"
          test -n "$pin"
          mkdir -p third_party
          rm -rf third_party/llama.cpp
          git init third_party/llama.cpp
          git -C third_party/llama.cpp remote add origin https://github.com/ggml-org/llama.cpp
          git -C third_party/llama.cpp fetch --depth 1 origin "$pin"
          git -C third_party/llama.cpp checkout --detach "$pin"
          test -f third_party/llama.cpp/CMakeLists.txt
          test -f third_party/llama.cpp/include/llama.h
      - name: Prepare Qwen model
        shell: bash
        env:
          SOUCHASTNIK_MODEL_URL: ${{ secrets.SOUCHASTNIK_MODEL_URL }}
        run: |
          set -euo pipefail
          test -n "$SOUCHASTNIK_MODEL_URL"
          bash tools/prepare_model.sh
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace
      - name: Verify packaged model
        shell: bash
        run: |
          set -euo pipefail
          apk="app/build/outputs/apk/debug/app-debug.apk"
          test -f "$apk"
          size="$(stat -c%s "$apk")"
          echo "APK size: $size bytes"
          test "$size" -gt 100000000
          unzip -l "$apk" | grep -F "lib/arm64-v8a/libmodel-qwen35-08b-q40.so"
          unzip -l "$apk" | grep -F "lib/arm64-v8a/libsouchastnik.so"
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: souchastnik-debug-apk
          path: app/build/outputs/apk/debug/*.apk
